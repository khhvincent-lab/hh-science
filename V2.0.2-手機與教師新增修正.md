# 解題實驗室 v2.0.2｜學生手機首頁與教師新增帳號修正版

基礎版本：上一版已交付的 v2.0 完整升級測試版；包含 v2.0.1 學生首頁三項修正及本次教師帳號修正。原有學生資料、解題引擎、五套主題及四級管理員權限保留。

## 學生端（手機）

1. **上方品牌列不再蓋住內容**：品牌列在正常頁面排版中，不隨捲動固定浮在學生姓名或額度上方；中文品牌名稱優先顯示。
2. **首頁改為一屏優先**：把每日額度、上傳題目、四科選擇、參考答案、補充敘述與「開始解題」濃縮到手機首頁；移除重複額度和首頁內重複的學習紀錄快捷鍵。已上傳多張圖片可左右滑動縮圖、編輯與刪除。
3. **底部導覽改為三格**：首頁／解析／紀錄；選單只保留右上角。主題切換、使用教學、加入主畫面及登出仍由右上方功能區提供。

小手機、鍵盤開啟、多圖或長文字、圖片裁切及新手教學等情況，必要時**仍允許上下捲動**；不會為了勉強「一屏」而裁切按鈕或學生輸入。

## 管理後台（教師帳號）

4. **新增帳號不再「按了沒有反應」**：前端先檢查帳號格式、姓名、至少 10 碼的密碼和至少一間補習班；錯誤就顯示在「新增帳號」按鈕下方，不再藏在整個設定頁的頂端。送出時顯示「正在建立帳號…」，禁止重複點擊；伺服器錯誤也顯示同一處。
5. **後端診斷加強**：清楚區分登入過期、角色權限、同名帳號、補習班不存在、授權資料寫入失敗與資料庫表/欄位缺失。帳號建立成功但列表載入失敗時會明確告知「已建立，勿重複新增」。新增請求超時後，先檢查名單再重試。
6. **資料安全**：帳號新增沿用 v2.0 補習班層級權限；不調整學生、班級或解題紀錄。

## 是否需要再執行 SQL？

**本次 v2.0.2 不新增資料庫欄位，已執行過 v2.0 SQL 者不須重跑。** 若後台出現 `admin_users.deleted_at` / `admin_user_institutions` 不存在等訊息，表示之前的 `migration-v2.0.sql` 未在目前網站連線的 Supabase 專案完成；請備份後確認 SQL 是否已執行。ZIP 內仍附上之前的 `migration-v2.0.sql` 供核對。

## 覆蓋與測試

1. 先備份 v2.0 專案；保留自己的 `.env.local` 和 `.git`。
2. 將 ZIP 內 `hh-science/` 裡面的檔案覆蓋原本專案根目錄。
3. VS Code 終端機依序執行：

```bash
npm install
npx tsc --noEmit
npm run build
npm run dev
```

4. 本機或 Vercel Preview 使用手機測試頁首、首頁、三格導航、拍照／裁切／解題／追問／返回／紀錄；使用**總管理員**新增一個新測試教師，確認清單出現、可登入、僅能看到授權補習班。也測試空白帳號、短密碼、沒勾補習班、重複帳號有清楚錯誤。
5. 全部正常後才正式部署：

```bash
git add .
git commit -m "v2.0.2 - Fix mobile student layout and teacher creation"
git push
```

## 驗證限制

已執行 TypeScript 靜態檢查和 CSS 語法解析；無法連接你的正式 Supabase 來實際新增教師，也不能在目前的 Linux 沙盒完成 Next.js 正式 Build（缺少 Linux SWC 原生套件）。請以你的 Mac / Vercel Preview 的 Build 和不同角色實測為正式上線條件。
