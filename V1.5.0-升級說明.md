# 解題實驗室 v1.5.0｜Icon 與補習班標題升級

## 本次實際完成

- 以本次上傳且已選定的深藍實驗室圖案，取代學生端與管理端 PWA icon（192／512／maskable）、Apple touch icon、網頁 favicon，並更新 manifest 圖示版本以減少快取影響。
- 學生頁面版本號更新為 v1.5.0；PWA Service Worker 版本同步更新。
- 「系統與教師 → 品牌設定」增加各補習班獨立顯示標題，可設定或清空恢復全站品牌。學生登入時所選的補習班決定顯示標題。
- 不改動既有學生、題目、登入、解題、教師權限與模型資料。

## 部署操作

1. **先備份**：保存線上程式碼和 Supabase 資料庫備份。
2. 在 Supabase → SQL Editor 執行根目錄 `migration-v1.5.0.sql` **一次**，在 `institutions` 表加入 `brand_title` 欄位。若不先執行，組織讀取可能報欄位不存在。
3. 用 ZIP 裡 `hh-science` 的專案檔案覆蓋原本 VS Code 專案的對應檔案，或以此目錄為新專案；**不要刪除既有本機 `.env.local`**。此交付包刻意不包含環境變數、node_modules、.next、.git。
4. 提交 GitHub，等待 Vercel 部署完成。無須改網域 DNS。
5. 部署後測試學生端、管理端，以及每個補習班的自訂標題。既有手機主畫面捷徑可能沿用舊 icon，移除並重新加入通常可刷新。

## 功能範圍聲明

此包確實完成 **icon + 每補習班標題** 的 v1.5.0 更新；先前提到的新增跨補習班分級管理員、補習班管理員可自行建立教師等完整角色與安全資料隔離，**本包尚未新增**，避免在沒有做完所有 API 權限驗證前將部分權限功能誤當成已上線。

## 驗證

執行 `./node_modules/.bin/tsc --noEmit --pretty false` 通過；尚未連接實際 Supabase／Vercel 帳號做線上端到端驗證。
