# 解題實驗室 v2.0 — 以 v1.5.1 為基礎的升級包

## 更新範圍

- 五套主題：A 靜謐深藍（預設）、B 森語晨光、C 極光藍境、D 墨夜流金、E 玄霧石墨；於學生與教師管理端切換。舊的六套主題儲存值自動對映到新五套主題。
- 學生登入、首頁、上傳卡片、科目選擇、解析卡片視覺更新。保留原本四個自然科、拍照與裁切、參考答案、補充敘述、教師導覽及 AI API。
- 「開始解題」按下後改為獨立的結果畫面：顯示 AI 載入、答案、觀念、詳解、選項分析和追問；左上方可返回，返回不清除題目。
- 教師與管理員登入全面改為品牌雙欄卡片（手機改單欄）。後台表格、側邊欄及卡片採五套主題。
- 四級權限仍由後端決定；教師授權由個別班級改為完整補習班（含未來新班級）。帳號頁只顯示「地區｜補習班」，可停用、重設密碼和軟刪除帳號。
- 學生與管理端 PWA 名稱仍然是「解題實驗室」，保留原版深藍 Icon。

## 升級步驟

1. 請先備份正式資料庫和目前正常運作的 v1.5.1 專案。保存原有 `.env.local`、`.git`（本 ZIP **刻意不包含**敏感環境變數、Git 歷史與 node_modules）。
2. 在正確的 Supabase 專案 SQL Editor 執行 **migration-v2.0.sql**。此 SQL 只適用於已經完成 v1.5.1 四級權限表的資料庫。
3. 將 ZIP 內 `hh-science/` **裡面的檔案**覆蓋原 VS Code 專案根目錄。不要形成 `hh-science/hh-science/`。
4. 在 VS Code 專案根目錄執行：

   ```bash
   npm install
   npx tsc --noEmit
   npm run build
   npm run dev
   ```

5. 使用本機或 Vercel Preview 分別測試學生、教師、補習班管理員、跨補習班管理員及總管理員。確認各個角色**無法**讀取未授權的補習班資料。確認學生裁切／解題／追問／分享及新手導覽正常。
6. 通過上述測試後才 `git add . && git commit -m "v2.0 - Five Themes and Institution Permissions" && git push`。確認 Vercel 顯示 Ready；有需要可使用既有部署的 Rollback。

## 注意事項

- 教師「刪除」是封存停用，紀錄保留；已封存的帳號名稱仍被原本的 unique constraint 保留，不會自動讓同名新帳號重複建立。請使用新帳號名稱。
- 如果 SQL 未執行就先部署程式，`admin_users.deleted_at` 查詢會失敗，教師帳號列表無法讀取，因此一定先執行 SQL。
- 本次未重新設計 AI 演算法或放寬 v1.5.1 原本刻意關閉的跨補習班全站 AI 設定與教學知識庫權限。
- 目前執行環境已通過 TypeScript 靜態檢查；`next build` **無法在本環境完成**，因為原 ZIP 的 node_modules 為 macOS 平台，此 Linux 環境無法連上 npm registry 下載 Linux SWC。請在 VS Code 或 Vercel Preview 補跑 build 與實際流程測試。
