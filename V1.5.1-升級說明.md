# 解題實驗室 v1.5.1 — 帳號四級分權＋主畫面名稱修正

> 以 v1.5.0 Icon 修正版為基礎。深藍 Icon、網址、學生 PIN 流程、AI 模型與 API Key 維持原本設定。

## 分權範圍

| 角色 | 授權與功能 |
|---|---|
| 總管理員 super_admin | 全站班級學生、全站設定、各補習班顯示標題、所有角色／帳號、可切換教師視角。 |
| 跨補習班管理員 platform_admin | 只看指定的多間／單間補習班與所屬班級學生；可建立及管理其範圍內的補習班管理員和教師。不得改動全站設定和其他補習班。 |
| 補習班管理員 institution_admin | 限自己獲授權的一間補習班；管理班級、學生、教師帳號。不可建立其他管理員。 |
| 教師 teacher | 僅檢視、管理獲授權班級的學生和相關解題紀錄、待修正紀錄。不可管理帳號或全站設定。 |

**重要限制：** 本次將未逐項完成租戶隔離的全站 AI 設定、教學規則、知識庫、成本分析等 API 限制為**總管理員**使用。非總管理員在這些端點會收到 HTTP 403，不會意外看到其他補習班的資料。教師校正中的「儲存為全站教學範例／規則」尚不開放給教師，須先設計補習班獨立知識庫才能安全開放。這是有意的安全限制，不是完成了所有角色均可使用所有舊功能。

## 更新順序

1. 保留你目前已上線的整份專案及 Supabase 備份，並備份 `.env.local`。
2. 先確認 Supabase 已執行過原本 `migration-v1.5.0.sql`（機構 `brand_title`）。
3. 在 **正確的 Supabase 專案** SQL Editor 執行本 ZIP 中 `migration-v1.5.1-roles.sql`。應顯示 Success。此 SQL 會保留既有教師班級授權，並自動建立其補習班關聯。
4. 解壓縮 ZIP，**複製 hh-science 裡的檔案**覆蓋你 VS Code 原有專案根目錄；不要複製成雙層資料夾，且**不要刪除 `.git` 或 `.env.local`**。
5. VS Code 在專案根目錄執行 `npm install`、`npm run build`。若有錯誤請先停止，不要推正式站。
6. 先在測試／Preview 環境登入總管理員，進入「系統與教師」→「管理員分級與教師帳號」建立測試帳號。
7. 用跨補習班管理員、補習班管理員、教師**分別登入無痕瀏覽器**，測試合法和不合法的補習班／班級／學生操作是否分別成功與回傳 403。
8. 驗證完成後提交 GitHub，確認 Vercel Production 顯示 Ready；檢查學生拍照解題及教師登入。

## 帳號設定

- 保留現有 `ADMIN_PASSWORD` 與其他環境變數；首次 `admin` 以既有密碼登入時，既有自動建檔流程會使用 bcrypt 保存正式帳號。
- 帳號初始密碼 10–128 碼；所有正式管理員與老師可以在側邊欄「修改我的密碼」自助修改，需先輸入原密碼。
- 停用帳號、變更角色或密碼之後，舊登入在下一次管理 API 請求會失效（自助變更密碼會清除 cookie）。
- 舊的 legacy session 需要重新登入一次。
- 任何帳號都不能從網頁自行提高自己的角色。

## 主畫面名稱

學生與管理端 `manifest` / Apple Web App Title 改為「解題實驗室」；網址、Icon 圖像未更換。iPhone 已安裝捷徑通常需要移除後重新加入主畫面，才能看到新名稱。

## 驗證範圍與上線注意

此交付版本已通過 `tsc --noEmit` 型別檢查。容器內 `next build` 因無法連到 npm registry 下載 Linux SWC 執行檔而未能跑完；**不等於 Vercel 實際部署通過**。也沒有連接使用者 Supabase，無法真實執行 SQL 或驗證生產資料，故請先完成上述 Preview 測試再更新正式網站。
