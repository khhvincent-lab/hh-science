# 解題實驗室 v2.0.3｜學生端手機操作與詳解互動修復

此完整專案以已交付的 **v2.0.2 手機與教師新增修復版 ZIP** 為基礎，不重建學生帳號或解題歷史，保留五種主題與四級管理員權限。

## 這次修正

1. **上傳圖片後立即編輯**：手機點選圖片即開啟獨立的全螢幕裁切工作區，旋轉、拖曳裁切框、確認、取消都位於同一個可見工作區。不再要先滑過學生資訊及巨大上傳卡才找到裁切器。完成後保留目前位置；已上傳圖片以橫向縮圖卡呈現。桌機仍保留原有編輯操作。
2. **一屏優先的解題首頁**：學生資訊只顯示一組姓名／班級／今日額度摘要；手機維持三格底部導覽。縮短首屏各卡片、欄位、間距。小螢幕、裁切、多圖、系統字級放大及鍵盤開啟時仍允許正常捲動，避免按鈕消失。
3. **解題紀錄精簡**：進紀錄頁不再顯示大型學生額度卡。預設僅顯示關鍵字搜尋、搜尋、進階篩選切換；科目、開始／結束日期、收藏、套用及清除全部收在進階篩選。檢視單筆紀錄時隱藏搜尋區，讓解析更快出現；手機縮小紀錄縮圖與清單間距。
4. **互動詳解數量與可見性**：AI 指示調整為「資訊足夠時 8～12 個有學習價值的重點」；不強迫簡單題湊數或編造內容。詳解與歷史紀錄在正文後列出 AI 實際回傳的所有註解按鈕，解決部分標註未嵌入公式而找不到可點選位置的問題；點擊保留原本「是什麼／從哪裡來／為何使用」的完整說明。
5. **註解視窗配色**：綠、黃、紅三色淺色解釋卡統一使用深色文字，包含 LaTeX 元素，避免白底白字；修正五套主題的文字可讀性。

## 不變的部分

- 不調整任何 Supabase 表格，已執行 v2.0 SQL 的網站**本次不用再執行 SQL**。
- 教師新增、帳號刪除、四級權限與補習班授權沿用 v2.0.2。
- AI 解題引擎的模型選擇、資料儲存與追問額度機制不更動；只有詳細解釋的指示密度提升。
- 圖片數量最多 5 張，檢查、旋轉與裁切仍沿用現有 Cropper。

## VS Code 部署

先備份原本 v2.0.2 資料夾。把 ZIP 內 `hh-science/` 的檔案覆蓋到 VS Code 專案根目錄，**保留自己的 `.git` 和 `.env.local`**；不要把整份新版資料夾多包一層。執行：

```bash
npm install
npx tsc --noEmit
npm run build
npm run dev
```

本機及 Vercel Preview 先用 iPhone Safari／加入主畫面測：上傳相片 → 直接裁切 → 確認 → 編輯縮圖 → 選科目／解題；查看紀錄預設收合／展開／單筆詳解；各五套主題點選互動重點，尤其檢查彩色說明卡文字。

測試成功再部署：

```bash
git add .
git commit -m "v2.0.3 - Compact mobile workflow and annotation contrast"
git push
```

如更新後仍看到舊頁面，請先 Safari 重新整理、關閉並重新開啟主畫面 PWA；必要時清除網站快取／重新加入主畫面，以免 Service Worker／手機快取持續顯示前版 CSS。切勿清除正式 Supabase 資料。

## 此環境的驗證狀況

已執行 TypeScript `tsc --noEmit` 與 `postcss` CSS 解析；Next.js `npm run build` **未通過此 Linux 環境檢查**，原因是現有依賴資料夾缺少 Linux 原生 SWC 套件，並非已證實的程式碼編譯錯誤。正式上線以前仍須在你的 Mac 或 Vercel Preview 完成 Build 及手機實測。未連線真實 Supabase，也未宣稱完成線上部署。
