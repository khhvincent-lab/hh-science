# 解題實驗室 v2.0.4｜手機閱讀與五套質感主題

本分支從 GitHub main 的 v2.0.3 程式建立，採非破壞式調整，保留學生、老師登入、上傳圖片裁切、每日額度、追問與解題紀錄資料庫。

## 學生首頁
- 舒適型手機排版：放大學生資訊卡、上傳區、輸入框與主要按鈕，保留小螢幕的自然垂直捲動。
- 科目四欄一排，畫面名稱「地科」；底層科目值仍為 `earth`，不改資料庫欄位、模型科目語意。
- 額度資訊只在右側顯示一次，移除名稱下方的重複「今日剩餘」文字。
- 已上傳圖片沿用縮圖取代上傳區與全螢幕編輯器，不增設額外大型卡片。
- 五套主題以各自主色增加霧面漸層、柔光、科學弧線、高光與細描邊。

## 解析與紀錄
- 主要內容維持「正確答案 → 觀念詳解 → 選項解析 → 追問」。
- 移除詳解、歷史詳解下方的「點選理解重點」整組快捷按鈕；保留內文既有的可點擊公式與註解。
- 觀念與公式容器不再提供整塊左右滑動；長 KaTeX 公式按運算基底換行，避免直接斷開數學符號。
- 歷史搜尋維持關鍵字優先，進階篩選預設收合，縮小手機列表區。
- 註解說明的淡色卡片維持深字對比。

## 版本與部署
- `package.json`、`package-lock.json`、`public/sw.js` 更新至 v2.0.4。
- 沒有資料庫 schema 變更，不需要執行 SQL。
- 此 PR 預計先在 Vercel Preview 測試，再視測試結果合併 main。
- 測試重點：iOS Safari/PWA 與 Android Chrome 的安全區、上傳→裁切→返回首頁、四科一列、長公式、紀錄詳解、五主題文字對比。
